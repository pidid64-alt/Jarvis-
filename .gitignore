# Python environments and generated caches
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
# Local secrets and runtime artifacts
.env
*.log
# Cloned and built by install.sh — not vendored in this repo
whisper.cpp/
